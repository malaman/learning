
const initialState = {
    home: 'HOME CONTENT',
    about: 'ABOUT CONTENT'
};

export default function user(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
